{"date":"2026-05-09","problem":"Agent planned Claude Code-specific worktree tooling (EnterWorktree, ExitWorktree) for skill instructions, violating the existing agent-agnostic rule in AGENTS.md","layer":".agents/reference/skill-writing-guide.md","fix":"Added Pre-Plan Checklist to skill-writing-guide.md with agent-agnostic check as item #1, requiring AGENTS.md guardrails to be actually read before any skill design begins","prevents":"Claude Code-specific tooling being designed into skills that should work on any coding agent"}
{"date":"2026-05-11","problem":"Created 3 new skill directories directly in ~/.claude/skills/ (wrong location) instead of in the git repo at ~/Sites/aiskills/ and symlinking. Files worked for Claude Code but were outside git and silently untracked. aet-review passed without catching it because no lens checked file location.","layer":".agents/reference/skill-writing-guide.md + aet-review/SKILL.md","fix":"Added 'New-file location verified' item to skill-writing-guide.md Pre-Plan Checklist requiring ls -la before mkdir to detect symlink structure. Added 'Project Structure' as the first review lens in aet-review, requiring ls -la on any parent directory where new files were created.","prevents":"New files created outside the git repo in projects that use symlink indirection between the tool-read directory and the tracked source directory."}
{"date":"2026-05-12","trigger":["git branch","stacked PR","aet-ship"],"problem":"Stacked PR base not updated after parent merged. S2-T2 was stacked on S2-T1. After S2-T1's PR merged to main, S2-T2's PR still targeted the feature branch. No signal surfaced. S2-T2 landed in the wrong branch and required a manual recovery merge.","layer":"~/.claude/skills/aet-ship/SKILL.md","fix":"Added stacked branch detection step to aet-ship ship procedure (between Sync with main and Run test suite). Detection: compare git merge-base HEAD main vs git rev-parse main. If different, branch is stacked. Warning injected into PR body and printed as terminal stop-note. No auto-rebase — human acts at PR review time.","prevents":"Stacked PRs silently merged against a stale base after the parent branch lands in main."}
{"date":"2026-05-23","trigger":["aet-work","queue","plan drift"],"problem":"aet-work queue drift: new plan files added to docs/plans/ were invisible to the queue because status/next/run never compared the queue against disk. 7 orphaned plans created a false all-clear signal.","layer":"aet-work/SKILL.md","fix":"Added plan-drift command to detect orphaned plans and stale queues. Updated init-queue to merge instead of overwrite. Made status/next/run refuse to proceed when drift is detected.","prevents":"Untracked follow-up plans, recovery work, or cleanup tasks being silently ignored because the queue reports 100% completion."}
{"date":"2026-05-23","trigger":["planning","approval","implementation gate"],"problem":"Agent interpreted user's 'yes' to a planning question as carte blanche to implement 3 skill file changes immediately. No plan.md was written. No explicit scope confirmation was sought.","layer":"AGENTS.md + .agents/commands/approval-checkpoint.md","fix":"Added 'Design-to-implementation hard gate' to AGENTS.md Agentic Workflow Guardrails. Created .agents/commands/approval-checkpoint.md as a reusable procedure requiring explicit file enumeration and approval before any implementation.","prevents":"Implied approval escalation where free-form design conversations ('yes', 'sounds good') are treated as implementation approval."}
{"timestamp":"2026-06-01T18:00:30+01:00","trigger":["orchestrator","background job","timeout"],"context":"aet-work orchestrator","lesson":"Unattended orchestrators must use the most headless CLI mode (e.g. --afk, not --yolo) so approval gates auto-dismiss in background jobs with no terminal. Default Shell timeouts (60s foreground, 15min background) are too short for aet-pipeline-implement tasks that run 30-60 min each. Always specify an explicit timeout (e.g. 7200s) when spawning the orchestrator.","source":"docs/bugs/2026-06-01-orchestrator-timeout-bug-report.md"}
{"date":"2026-06-10","trigger":["orchestrator","unattended","commit"],"problem":"aet-work orchestrator marked 5 of 13 tasks as 'done' even though their branches had zero commits. The agent wrote code but failed to commit before exiting. The orchestrator only checked exit code 0, not whether the branch had actual commits.","layer":"aet-work/references/orchestrator-template.sh","fix":"Added verify_branch_has_commits() function to the orchestrator template and generated script. After a task exits 0, the orchestrator now verifies git rev-list --count main..HEAD > 0 before marking 'done'. If the branch has uncommitted changes but no commits, the task is marked 'failed' with failed_stage: uncommitted.","prevents":"Empty branches being marked as complete in unattended mode. Forces the agent to commit or fail loudly."}

{"date":"2026-06-11","trigger":["reasoning","principles","options","analysis","ADR","centralization"],"problem":"Agent correctly identified that per-project aet-state.py violated ADR 003's toolkit-level centralization principle, then undermined its own conclusion by offering 'leave it' and 'hybrid' options that preserved the violation.","layer":"AGENTS.md","fix":"Added 'Analysis-to-action discipline' guardrail to AGENTS.md Agentic Workflow Guardrails. When analysis proves a principle violation, state the conclusion and propose the fix. Do not present options that preserve a known error.","prevents":"Status-quo bias leaking into recommendations after a clear analytical conclusion, forcing the user to re-state obvious fixes."}

{"date":"2026-06-11","trigger":["orchestrator","CLI","kimi","claude","--afk","--work-dir","--cwd"],"problem":"The orchestrator's CLI adapter declared three flags that do not exist in the actual binaries: kimi --afk, kimi --work-dir, claude --cwd. This caused every orchestrator spawn to fail with 'unknown option'. The --afk assumption had also leaked into docs, bug reports, and learnings.","layer":"aet-work/lib/cli_adapter.py + aet-work/lib/queue.py","fix":"Updated cli_adapter.py to use real flags: kimi --yolo (headless), claude --dangerously-skip-permissions (headless), workdir_flag=None for both (cwd handled by subprocess). Updated queue.py to preserve dict-wrapper queue format instead of stripping metadata on write. Updated tests, reference script comments, and added a correcting learning.","prevents":"Orchestrator spawning sessions with fake CLI flags, and metadata loss when the queue file uses a dict wrapper."}
{"date":"2026-06-16","trigger":["aet-work","aet-ship","worktree","origin/main","PR scope","package-deliverable"],"problem":"aet-work created worktrees from the parent session branch instead of origin/main, and aet-ship opened PRs against a stale local main. Eight PRs contained ~220 changed files each, almost none related to their tasks. Manual rebase and cleanup was required. An initial fix also placed a hygiene rule in .agents/reference/, which is not packaged with the skills and therefore invisible to projects that install them.","layer":"aet-work/lib/worktree.py + aet-ship/SKILL.md + docs/CONVENTIONS.md + .agents/reference/skill-writing-guide.md","fix":"Updated aet-work create_worktree() to fetch origin/main and create worktrees from origin/main, recreating any existing branch whose merge-base is wrong. Removed the project-specific app/node_modules symlink. Updated aet-ship to fetch origin, compute the PR base against origin/main, conditionally rebase independent branches onto origin/main before opening PRs, detect stacked branches by origin/main, and add a scope-audit section to the PR body. Added a package-deliverable rule to docs/CONVENTIONS.md and to skill-writing-guide.md checklists: every rule a skill enforces must live inside the skill's packaged files. Removed the toolkit-internal .agents/reference/worktree-ship-hygiene.md.","prevents":"Unrelated commits leaking into PR diffs, out-of-scope files in PRs, project-specific assumptions in generic skills, and skill rules that cannot be delivered to projects because they live outside the skill package."}
{"timestamp":"2026-06-17T19:15:00Z","trigger":["aet-validate-scope","scope-validated","plan files","work queue"],"problem":"aet-validate-scope marked a PRD as scope-validated without verifying that the PRD had been broken into docs/plans/*.md files or that those plans were synced into .agents/work-queue.json. The pipeline ended with a validated PRD but no queued tasks, so implementation could not start.","layer":"aet-validate-scope/SKILL.md + aet-pipeline-plan/SKILL.md","fix":"Add a closure check to aet-validate-scope: before updating the PRD footer to scope-validated, verify that at least one docs/plans/*.md file exists for the PRD and that all such plans appear in .agents/work-queue.json. If not, stop and redirect to aet-plan / aet-work sync. Update aet-pipeline-plan Step 3 to verify sync via aet-work status before declaring the pipeline complete.","prevents":"Validated PRDs being left without implementation plans, and planning sessions ending before the work queue reflects the planned work."}
{"timestamp":"2026-06-17T23:20:00Z","trigger":["aet-qa","cli_adapter","kimi","headless_flag","regression test"],"problem":"The `kimi` CLI rejects combining `-p/--prompt` with `--yolo` or `--auto` with the error 'Cannot combine --prompt with --yolo'. Treating `--yolo` as the headless flag when the adapter also uses `-p` produces a command that fails immediately, blocking all orchestrator spawns.","layer":"aet-work/lib/cli_adapter.py + tests/test_cli_adapter.py","fix":"Set `kimi` adapter `headless_flag` to an empty string so headless mode only emits `kimi -p <prompt>`. Added regression test `test_kimi_headless_build_cmd` that asserts the built command does not contain `--yolo` or `--auto` and equals `[\"kimi\", \"-p\", \"run tests\"]`.","prevents":"Headless kimi invocations using an invalid flag combination and regressions in CLI adapter definitions reaching later stages."}
{"date":"2026-06-18","trigger":["orchestrator","hang","timeout","watchdog","subprocess","batch"],"problem":"aet-work run orchestrator hung after marking all tasks complete. Output log stopped advancing, summary never printed, and the process had to be killed manually. There is no per-task timeout, heartbeat, or guaranteed cleanup/summary path.","layer":"aet-work/bin/orchestrator","fix":"Documented in docs/retros/2026-06-18-orchestrator-hang-no-recovery.md. Planned fix: add per-task wall-clock timeout, heartbeat logging, total batch timeout, and guaranteed cleanup/summary via try/finally.","prevents":"Silent orchestrator hangs after task completion and orphaned worktrees/branches when the process is killed."}

{"timestamp":"2026-06-18T17:15:00Z","trigger":["aet-work","worktree","untracked","docs/adr","copy_untracked_files"],"problem":"aet-work copy_untracked_files() only copied untracked files from docs/plans/ and docs/prds/, so an ADR authored in the same session and referenced by a plan never reached the implementation branch. The allowlist silently dropped docs/adr/ and other docs artifact types a plan can depend on.","layer":"aet-work/lib/worktree.py","fix":"Expanded the git ls-files pathspec in copy_untracked_files() to include docs/adr/, docs/audits/, docs/retros/, and docs/product-briefs/, and updated the docstring. Added a regression test in tests/test_worktree.py that creates an untracked plan, PRD, ADR, audit, retro, and product-brief and asserts all are copied into the worktree.","prevents":"Untracked ADRs and other referenced docs being left behind when a plan is implemented in a worktree, which could ship code whose governing decisions are missing from the branch."}
{"timestamp": "2026-06-18T22:38:50.653642+00:00", "trigger": ["aet-work", "orchestrator", "gitignore", "execution.log.jsonl", "dirty-tree"], "problem": "aet-work run-one/run halts with Working tree is dirty because orchestrator-generated logs (.agents/execution.log.jsonl and aet-work-*.log) are not gitignored. The orchestrator creates these files but refuses to run if any untracked file exists.", "layer": ".gitignore + aet-setup + aet-work/SKILL.md", "fix": "Added .agents/execution.log.jsonl, aet-work.log, and aet-work-*.log to .gitignore. Updated aet-setup/SKILL.md and aet-setup/checklist.md to require these patterns when scaffolding .agents/ workflow infrastructure.", "prevents": "Orchestrator dirty-tree halts on its own telemetry artifacts in fresh or upgraded projects."}
{"timestamp":"2026-06-18T23:58:00Z","trigger":["queue","dict-wrapper","read_queue","write_queue"],"problem":"read_queue/write_queue lost the dict-wrapper envelope when the only key was 'tasks'. The extracted wrapper was empty, so write_queue fell back to a plain list, corrupting queue files that started as {'tasks': [...]}.","layer":"aet-work/lib/queue.py","fix":"Track whether the source file was a dict wrapper (not just its metadata) in _queue_wrappers, so write_queue emits a dict envelope even when there is no additional metadata.","prevents":"Queue format corruption after seal_terminal or any read-write cycle on a minimal dict-wrapper queue."}
{"timestamp":"2026-06-19T10:06:46Z","trigger":["aet-work","aet-state","merged","merge_commit","branchless"],"problem":"aet-state validate_transition rejected merged transitions when a task had no branch, even if a merge_commit was recorded and was an ancestor of origin/main. This blocked archiving branchless historical work that had already been merged to main.","layer":"aet-work/bin/aet-state","fix":"Removed the hard branch requirement in validate_transition; merged status is now accepted when either branch or merge_commit is an ancestor of origin/main.","prevents":"Branchless merged tasks being stuck unverifiable, which prevented archival of historical work merged without a retained local branch."}
{"timestamp":"2026-06-19T17:17:39Z","trigger":["aet-work","sync","legacy","frontmatter","blocked_by","append-only"],"problem":"aet-work sync failed because it validated the entire docs/plans/*.md corpus against the fods-05 fail-closed intake contract. 80 legacy plans had valid frontmatter blocked_by but still contained a ## Blocked by / ## Dependencies prose section, so sync exited without adding the 7 new candidate plans.","layer":"aet-work/bin/sync + aet-work/lib/plan_parser.py","fix":"Made sync append-only at the validation layer: it now parses all plans for cross-plan ID resolution but only reports intake errors for candidate plans (not already queued or settled). Also relaxed the legacy-section check so a ## Blocked by / ## Dependencies section is fatal only when frontmatter does not explicitly declare blocked_by.","prevents":"Historical plan drift from blocking new work from entering the queue, and avoids requiring manual queue edits when legacy prose sections remain after a frontmatter migration."}

{"timestamp":"2026-07-06T19:25:01Z","trigger":["planning","granularity","docs/plans","templates","examples"],"problem":"The aet-setup reference-evolution PRD was decomposed into one plan per example template. Most plans ended up as 2-8 line diffs (plan-stage bookkeeping + a link fix) because the templates were already on main, producing 8 PRs where 2-3 would have sufficed.","layer":"docs/CONVENTIONS.md Task Size Guardrails + .agents/templates/plan-template.md","fix":"Added a Batching Rule to CONVENTIONS.md and a Batching Check to the plan template, prompting planners to group near-identical small additions into one plan/branch/PR.","prevents":"Wasted branch/PR overhead and duplicated drive-by fixes across multiple tiny PRs."}
{"timestamp":"2026-07-06T19:25:01Z","trigger":["aet-work","orchestrator","dirty main","work-queue.json","hygiene"],"problem":"`aet-work run` halted on the resumed batch because `.agents/work-queue.json` is tracked and the orchestrator's main-hygiene check treats the queue mutations it just wrote as a dirty working tree.","layer":"aet-work orchestrator / repository tracking policy","fix":"Documented in retro. Short-term workaround is `AET_EXECUTION_MODE=unattended`; longer-term fix requires either ignoring the queue file in hygiene or removing it from git tracking.","prevents":"Orchestrator runs failing immediately after queue state updates and leaving tasks in inconsistent states."}
{"timestamp":"2026-07-06T19:25:01Z","trigger":["make validate","broken link","docs/upgrades/README.md","duplicated fix"],"problem":"A pre-existing broken relative link in `docs/upgrades/README.md` was rediscovered and fixed independently in multiple feature branches because `make validate` failed on it for every branch.","layer":"main-branch health / validation hygiene","fix":"Fix repo-wide broken links on `main` as soon as `make validate` starts failing; do not leave them for each feature branch to rediscover.","prevents":"Same one-line fix appearing in multiple unrelated PR diffs and confusing scope audits."}
{"timestamp":"2026-07-06T19:25:01Z","trigger":["aet-evolve","mine-learnings","CLI","import"],"problem":"`mine-learnings` crashed on launch with `NameError: name 'sys' is not defined`, so it could not scan telemetry for recurring patterns.","layer":"aet-evolve/bin/mine-learnings","fix":"Added missing `import sys` and committed the fix. Add a smoke import/exit-code test for CLI entry points.","prevents":"Evolution tooling failing silently and leaving recurring patterns unmined."}
{"timestamp":"2026-07-09T08:22:24Z","trigger":["aet-work","orchestrator","NameError","timeout","queue drift"],"problem":"aet-work run on a 4-task batch crashed mid-batch with NameError: read_queue, and each task repeatedly hit the 10-minute foreground/background timeout. The orchestrator had no per-task timeout, no heartbeat, and no cleanup/summary guarantee, leaving the queue in failed/in_progress states even after tasks were manually merged to main.","layer":"aet-work/bin/orchestrator + aet-work/lib/worktree.py + aet-work/bin/aet-state","fix":"Add per-task wall-clock timeout and heartbeat logging to the orchestrator; guarantee cleanup/summary via try/finally. Refresh origin/main in create_worktree before reuse and rebase existing worktrees when origin/main advances. Allow aet-ship/bin/ship to close tasks from a CLI-supplied branch or merge commit when queue branch is null, and add a queue self-heal/audit command that reconciles queued tasks against git ground truth.","prevents":"Multi-task batches silently crashing or timing out, stale origin/main polluting PR diffs, and manual task closure fighting queue metadata."}
{"timestamp": "2026-07-09T22:49:54Z", "trigger": ["aet-work", "orchestrator", "frh-07", "false-completion", "awaiting_merge"], "problem": "The orchestrator marked frh-07-status-retirement-state-bin as awaiting_merge within seconds with no commits and no captured agent output. The worktree was removed by remove_worktree, leaving an empty branch and a stale worktree warning.", "layer": "aet-work/bin/orchestrator task finalization + agent adapter", "fix": "Add an exit-code is not enough gate: before transitioning a task to awaiting_merge, verify the worktree has at least one commit ahead of origin/main and the plan footer advanced to a non-plan-approved stage. Surface agent stdout/stderr when a task returns too quickly.", "prevents": "Empty branches being merged/verified and the queue filling with bogus awaiting_merge tasks that block dependent auto-unblock logic."}
{"timestamp":"2026-07-09T23:10:52Z","trigger":["aet-work","orchestrator","frh-07","false-completion","stale-footer"],"problem":"Batch children received no stored pipeline stage, so they relied on the plan footer in the reused worktree. A stale terminal footer (e.g., _Stage: synced_) made process_task skip every stage and return success; the parent then promoted the empty branch to awaiting_merge.","layer":"aet-work/bin/orchestrator","fix":"Pass AET_STAGE from the batch parent to the child; refuse to complete a task when no agent stage was invoked and the branch has zero commits; verify commits in run_single and _finalize_task before promoting to awaiting_merge.","prevents":"Bogus awaiting_merge tasks with empty branches that block the sprint and corrupt dependent unblock logic."}
{"timestamp": "2026-07-11T11:39:59Z", "trigger": ["aet-plan", "plan-split", "renumbering", "prd-consistency"], "problem": "Splitting install/self-repair out of cli-01 into a new cli-04 renumbered the sweep to cli-05, but only the PRD sizing note was updated: the end-state acceptance criterion, the rewrite-contract note, and the gate-decision line still attributed cli-05 deliverables (deletions, prune flip, one-name PATH) to cli-04, and G3 said six legacy PATH names against R-5's seven. Caught at queue-registration time, fixed before commit.", "layer": "docs/prds (planning docs)", "fix": "After any plan split/renumber, grep the PRD and brief for every old plan ID and for plan-count claims \u2014 the sizing note is never the only reference. Attribute end-state acceptance criteria to the plan that actually delivers them.", "prevents": "Phase exit gates verifying the wrong plan's merge, and implementing agents reading contradictory plan attribution."}
{"timestamp": "2026-07-11T13:26:24.760627Z", "trigger": ["aet-work", "orchestrator", "telemetry", "external-deletion", "FileNotFoundError", "wfd-03"], "problem": "While `aet-work run` was live, an out-of-band cleanup (another session pruning old telemetry) deleted the active run dir ~/.aet/telemetry/<slug>/<date>/<run-id>/. The task child crashed in RunLogger.append_record after completing its first stage group, and the parent crashed in write_last_run, so wfd-03 was marked failed and its 6-task dependent DAG stalled \u2014 even though every worktree commit (through qa-complete) was intact. The crash is in the bookkeeping layer, not the work.", "layer": "aet-work/lib/telemetry.py RunLogger writes vs external telemetry pruning", "fix": "Operational: never delete the live run's telemetry dir \u2014 check .agents/work-queue.lease for the active run_id (and pid liveness) before pruning ~/.aet/telemetry. Recovery when it happens: `aet-state transition <task> failed ready` \u2014 the worktree is reused and the run resumes from the plan footer stage. Candidate hardening (NOT applied; owner wants telemetry untouched): mkdir(parents=True, exist_ok=True) the run dir inside append_record/write_last_run/copy_work_history so a vanished dir is not fatal.", "prevents": "A sprint DAG stalling on a bookkeeping crash, and repeated failed->ready churn when telemetry maintenance runs concurrently with aet-work run."}
{"timestamp": "2026-07-11T21:37:57.384057+00:00", "trigger": ["aet-work", "orchestrator", "evidence-gate", "group-session", "AET_EVIDENCE_PATH", "thp-04", "false-failure"], "problem": "thp-04 was marked failed three times despite complete, verified work (footer synced, cso/sync-docs verdicts pass). Two stacked causes: (1) in the 4-way batch, the child exited 1 after all stages passed \u2014 the _record_stage write-back after the [reviewed,secure] group did not persist, so the task record stuck at qa-complete while the footer read synced; (2) on re-runs the cso gate failed closed ('missing cso verdict') because run_stage_group never sets AET_EVIDENCE_PATH (only single-stage run_stage does), so the agent fell back to evidence.write_verdict with the slug derived from the worktree CWD (~/.aet/reports/aiskills/<worktree>/<task>/<kind>.json) while the gate reads derive_project_slug(repo_root) = aiskills/main (~/.aet/reports/aiskills/main/<task>/<kind>.json). Same-repo path mismatch, both sides fail-closed.", "layer": "aet-work/bin/orchestrator run_stage_group env + _record_stage under batch concurrency", "fix": "Immediate recovery: copied the existing cso.json/sync-docs.json from the worktree-slug path to ~/.aet/reports/aiskills/main/thp-04-retention-prune-cli/, transitioned failed->ready, re-ran to pass the gates and promote. Code fix (NOT applied \u2014 needs a plan): set AET_EVIDENCE_PATH per evidence-bound stage in run_stage_group (e.g. AET_EVIDENCE_PATH_<KIND> or a group manifest the skills read), or make _load_checking_verdict fall back to the worktree-slug path; also make _record_stage retry/verify under concurrent batch children so a write-back failure cannot exit-1 a fully-completed task.", "prevents": "Re-run loops where completed tasks can never self-promote because group-session verdicts land on a different slug path than the gate reads, and false failures of completed tasks when stage write-back races under batch concurrency."}
{"timestamp": "2026-07-12T11:31:14.202483+00:00", "trigger": ["aet-work", "add", "pending_blockers", "deadlock", "work-history", "ewl-06"], "problem": "aet-work add seeded pending_blockers from the full blocked_by list without reconciling against work-history.jsonl, so a plan added after its blocker had merged and been archived entered blocked with a counter that nothing would ever decrement \u2014 a permanent, silent deadlock. heal reported 'No healable discrepancies' because archived blockers derived as unknown instead of terminal. Correctness depended on add-vs-merge ordering.", "layer": "aet-work/lib/plan_parser.py new_task_from_plan + aet-work/bin/aet-state _derive_all_states/cmd_heal", "fix": "new_task_from_plan accepts settled_ids and counts only non-settled blockers (zero pending -> insert ready); _derive_all_states seeds derived states from settled history so dependents of archived blockers derive ready; cmd_heal reconciles stored pending_blockers against blockers not terminal in queue or history (promotes deadlocked tasks, fixes overcounts like ewl-06 4->2 without a transition). Regression tests in tests/test_aet_work_add_review.py and tests/test_aet_state.py::TestHealHistoryAware.", "prevents": "Silent deadlocks when phase-gated plans are queued after their gating task merges (the common fast-moving-queue case), and stale counters re-deadlocking tasks later in the DAG."}
{"timestamp": "2026-07-12T17:28:24.867879+00:00", "trigger": ["aet-plan", "duplicate-plan", "ewl-02", "frh-18", "plan-graveyard", "owner-decision"], "problem": "ewl-02-evidence-path-group-session-fix was drafted inside the P3 PRD session while frh-18-group-evidence-path-contract \u2014 an independently-authored plan for the identical run_stage_group AET_EVIDENCE_PATH root cause \u2014 already existed, approved and queued. Duplicate planning surfaced only during clarify-goal grounding review.", "layer": "planning process: parallel plan authorship outside the PRD session is not visible until scope validation grounds the set against the live queue", "fix": "ewl-02 abandoned in frh-18's favor (per-kind AET_EVIDENCE_PATH_<KIND> precedence is the more correct fix); plan file deleted 2026-07-12 by owner decision rather than kept on disk \u2014 trail retained in the P3 PRD sizing note (docs/prds/roadmap-p3-enforcement-walls-prd.md) and this entry. Do not re-create an ewl-02 plan; R-3 is covered by frh-18 (merged).", "prevents": "Re-drafting an already-covered evidence-path fix, and treating a deleted plan ID as available for reuse in the P3 set."}
{"timestamp": "2026-07-12T20:47:58.205997+00:00", "trigger": ["aet-work", "queue", "integrity", "content_hash", "audit", "heal", "recovery"], "problem": "frh-17's tamper-evident queue envelope had no tool-provided recovery path: TaskBackend.load() hard-coded verify=True, so after any legitimate external edit to work-queue.json every loading command (audit, heal, transition, add, sync) raised QueueIntegrityError before doing work. The error message named `aet state audit` as the remedy, but audit died on the same check and never mutates, so it could not restamp the hash either. Only escapes were hand-recomputing the sha256 or deleting content_hash.", "layer": "aet-work/lib/queue.py read_queue + aet-work/lib/backends/*.py load + aet-work/bin/aet-state cmd_audit/cmd_heal/main", "fix": "Threaded verify through TaskBackend.load (JSON/GitHub backends pass it to read_queue; GitRefs accepts it as a no-op). audit is the diagnostic: loads unverified, warns, completes its report. heal is the repair: loads unverified; --apply restamps the envelope via a normal backend save before applying state fixes, preserving the external edits themselves. Error message now names audit (inspect) and heal --apply (repair); aet-state main() catches both QueueIntegrityError class instances (queue.py loads twice in-process as aet_queue and queue) and prints a one-line refusal instead of a traceback. Mutating commands stay fail-closed. Regression tests in tests/test_queue_guard.py (recovery section); contract recorded in ADR-024.", "prevents": "Self-DoS after legitimate external queue edits (migration scripts, manual curation), and error messages that name remedies which cannot run against the triggering condition \u2014 the remedy must be exercised in a test, not just detection."}
{"timestamp": "2026-07-12T21:24:21.246509+00:00", "trigger": ["aet-work", "orchestrator", "content_hash", "hygiene", "lock-file", "validations", "session-report"], "problem": "An aet-work session hit three hardening gaps: (1) next/sync/add/init-queue and batch startup still died with an uncaught QueueIntegrityError traceback on a tampered queue \u2014 ADR-024's clean refusal only wrapped aet-state, so the brick read as unrecoverable; (2) check_main_hygiene ignored only work-queue.json/work-history.jsonl, so the lingering work-queue.json.lock sidecar (never unlinked by design; unlinking an fcntl lock file races concurrent openers) tripped 'Working tree is dirty' in projects missing the gitignore entries, and aet-setup's template/checklist listed neither sidecar; (3) one-shot stage prompts never required foreground validations, so a stage agent backgrounded its own checks, ended its turn, and produced a zero-commit 'successful' session.", "layer": "aet-work/bin/{next,sync,add,init-queue,orchestrator} + aet-work/lib/worktree.py + aet-setup/{SKILL.md,checklist.md}", "fix": "Catch QueueIntegrityError at the first backend.load() in every mutating bin and at batch startup, returning a one-line refusal naming audit/heal (ADR-024). Add work-queue.json.lock and work-queue.lease to check_main_hygiene's ignored paths, the aet-setup gitignore template, and the checklist; document why the lock file is never unlinked. build_prompt and build_stage_group_prompt now require foreground validations that run to completion before commit-and-exit. Regression tests in tests/test_queue_guard.py (clean refusals), tests/test_worktree.py (sidecar hygiene + control), tests/test_orchestrator.py (prompt discipline + batch refusal). Bug doc: docs/bugs/2026-07-12-aet-work-session-hardening-gaps.md.", "prevents": "Traceback-on-tamper across every queue entry point, lock/lease sidecars bricking the clean-tree gate in fresh projects, and false-completion stage sessions that end with background validations still running."}
{"timestamp": "2026-07-12T23:45:05.6f+00:00", "trigger": ["aet-work", "orchestrator", "evidence-path", "frh-18", "fail-closed-gate", "group-session", "uct-01"], "problem": "run-one on uct-01-usage-cost-telemetry completed every stage successfully (implement/qa/review/cso/sync-docs all pass, 553 tests green, footer synced) but the orchestrator's post-group fail-closed gate rejected the run: qa.json and review.json were written to ~/.aet/reports/aiskills/main/uct-01-.../ while the reviewed\u2192secure\u2192synced GROUP session wrote cso.json and sync-docs.json to ~/.aet/reports/aiskills/uct-01-.../uct-01-.../ (no 'main' segment, doubled task id). The gate checked only the first scheme and failed closed on 'missing cso verdict'.", "layer": "evidence path resolution in aet-work/lib/evidence.py + orchestrator group-session env: per-stage sessions and group sessions resolve different verdict path schemes within a single run (frh-18's per-kind AET_EVIDENCE_PATH_<KIND> precedence not consistently applied); writer and gate reader disagree", "fix": "Not yet fixed. Interim: all four verdicts verified on disk (pass, correct task_id/stage), task left in_progress in queue, worktree branch holds the five stage commits, plan footer at synced \u2014 ready for aet-ship. Structural direction is already queued: ewl-01 (aet gate submit \u2014 single sanctioned verdict writer with canonical resolve_verdict_path) is the next unblocked plan after uct-01 ships. Do NOT hand-copy verdicts between path schemes as a workaround \u2014 that masks the contract bug.", "prevents": "Re-diagnosing this path split from scratch, and papering over it by duplicating verdict files into the scheme the gate happens to check."}
{"timestamp": "2026-07-13T09:06:36.319108+00:00", "trigger": ["aet-evolve", "aet-retro", "mine-learnings", "telemetry", "layout-drift", "slug-mismatch"], "problem": "`aet retro` produced a malformed report: mine-learnings scanned 16 runs but 0 files / 0 reports, Project-level findings were bare stage names (secure, reviewed, qa-complete...), and AET-level findings were raw Python dict reprs of June queue tasks. Root cause: telemetry layout drift. The current orchestrator writes ~/.aet/telemetry/{dirname}/{worktree-key}/{date}/{run-id}/{task}.jsonl (e.g. aiskills/main/2026-07-13/...), but aet-retro + mine-learnings still read the old scheme ~/.aet/telemetry/{owner}/{repo}/{date}/{run-id}/... \u2014 derive_project_slug() returned thebrightnest/ae-toolkit from the origin URL, so the retro parsed a stale old-scheme directory instead of the live one. Secondary bug: extract_message() treats every record as a finding \u2014 falls back to the `stage` key (bare stage names) then str(record) (dict reprs), with no record-type filtering. The run also polluted the archive: emit_learning_candidates appended those bogus findings as learning_candidate records under the wrong old-scheme path.", "layer": "aet-evolve/bin/aet-retro (derive_project_slug, recent_project_records, extract_message, categorize_records) + aet-evolve/bin/mine-learnings scan paths vs the orchestrator's telemetry writer (dirname/worktree-key layout)", "fix": "Not yet fixed. The malformed report at docs/retros/2026-07-13-aet-retro.md is untracked pending user decision. Direction: align retro/mine-learnings readers with the writer's layout ({dirname}/{worktree-key}/{date}/{run-id}/{task}.jsonl, worktree-key optional), derive the slug the same way the orchestrator does, filter records by type before treating them as findings, and never render str(dict) as a finding. Consider queuing as a plan alongside ewl-01 (evidence-path split) \u2014 both are telemetry/read-write contract drift.", "prevents": "Re-diagnosing retro garbage output from scratch, and trusting retro findings that are actually stale queue dumps or stage-name fallbacks. Also flags that learning_candidate pollution from bad retro runs lives under the old-scheme path."}
{"timestamp": "2026-07-13T13:18:23Z", "trigger": ["test-panel-plan-detail", "e2e", "fixture-drift", "live-archive", "empty-state"], "problem": "scripts/test-panel-plan-detail.mjs check 8 (run_summary-only wfd-01 plan row under project T/tmp) times out: the pytest-pollution fixture row it depends on no longer exists in the live archive. Verified pre-existing by stashing the lvp-01 panel changes and re-running \u2014 same failure on the old code.", "layer": "scripts/test-panel-plan-detail.mjs", "fix": "Not fixed (out of lvp-01 scope). Harness needs a self-built fixture (like test-panel-live-runs.mjs uses) instead of depending on live-archive pollution rows.", "prevents": "Mistaking archive drift for a regression when running the plan-detail E2E after panel changes."}
{"timestamp": "2026-07-14T10:28:44.656+00:00", "trigger": ["aet-state", "derive_status", "heal", "false-merged", "bare-branch", "ewl-04", "ewl-06", "ttf-01"], "problem": "derive_status (aet-work/bin/aet-state:158) rule 1 marks a task 'merged' whenever its branch tip is an ancestor of origin/main \u2014 but a failed run leaves the task branch as a bare pointer on the base commit (0 unique commits, never diverged), which also satisfies that check. ewl-04, ewl-06, and ttf-01 all misderived as merged while stored as ready; 'aet state heal --apply' trusts the derivation (aet-state:584) and would have transitioned ready -> merged, sealing two unimplemented plans as shipped. merge_commit was null and plan files still present in every case \u2014 the derivation ignores both signals.", "layer": "aet-work/bin/aet-state derive_status rule 1 (is_ancestor_of_main on bare branch) + cmd_heal's trust of the derivation", "fix": "Immediate (applied 2026-07-14): deleted the three bare branches after verifying 0 unique commits vs origin/main and merge_commit=null; all three now derive ready, matching stored state. Structural (NOT applied \u2014 needs a plan): rule 1 should require merge_commit set, or branch-on-main AND >0 unique commits vs origin/main; a bare base-pointer branch should fall through to the branch_exists/plan_exists rules. Candidate: queue as a small aet-work plan alongside the next derive/heal touch.", "prevents": "Silent archival of unimplemented work via heal --apply after any failed run, and re-diagnosing the same false-merged discrepancy from scratch for every killed batch."}
{"timestamp": "2026-07-14T13:20:00+00:00", "trigger": ["aet-work", "run-one", "sub-agent-timeout", "worktree", "slug-derivation", "gate-submit"], "problem": "`aet run-one` repeatedly timed out at the 300s foreground limit while the orchestrator's sub-agent executed aet-tdd→aet-implement, aet-qa, aet-review, and aet-sync-docs stages. Additionally, `aet gate submit` run inside a worktree derived the project slug from the worktree path (`aiskills/pfe-01-verdict-freshness-primitive`) instead of the canonical repo slug (`aiskills/main`), writing verdicts to a doubled path that the orchestrator's fail-closed gate would not read.", "layer": "aet-work/bin/orchestrator sub-agent adapter + telemetry.derive_project_slug + aet-work/bin/gate", "fix": "Completed the remaining stages manually: ran validations in the worktree, wrote verdicts via `aet gate submit` from the main repo (or directly via `evidence.write_verdict(project_slug='aiskills/main')`), updated plan footers, committed, and pushed. Transitioned queue state to awaiting_merge with `aet state transition`.", "prevents": "Abandoning a timed-out run-one halfway through a stage, and writing stage verdicts to a path the orchestrator cannot find when the stage runs inside a git worktree."}
{"timestamp": "2026-07-14T14:12:48Z", "trigger": ["aet-work", "aet add", "worktree", "origin/main", "untracked-plan", "durability", "hygiene", "unattended"], "problem": "aet add accepted plan files with ZERO git durability check: an untracked docs/plans/*.md entered .agents/work-queue.json and showed in aet status as a normal task. Because aet-work builds task worktrees from origin/main, a queued-but-uncommitted/unpushed plan spawns an EMPTY worktree ('plans go missing') \u2014 same class as docs/bugs/2026-06-18-orchestrator-untracked-adr-not-copied-to-worktree.md. Reproduced live: 5 untracked vgr-0* plans were queued while untracked. IMPORTANT CORRECTION: the initial hypothesis 'run-one skips check_main_hygiene' was WRONG \u2014 run_single enforces it for top-level runs (orchestrator:1515) and check_main_hygiene (worktree.py:335) catches both a dirty tree and main-ahead-of-origin; the real execution-side hole is enforce_main_hygiene (orchestrator:192) downgrading to a warn-and-continue when AET_EXECUTION_MODE=unattended.", "layer": "aet-work/bin/add (no intake durability guard) + aet-work/bin/orchestrator enforce_main_hygiene (softens to a warning in unattended/AFK mode) + aet-pipeline-plan/aet-plan (no commit-before-add step \u2014 the happy path leaves plans uncommitted through intake)", "fix": "Gap 1 fixed: aet add now refuses an untracked-in-git plan (bar = 'tracked' not 'committed+clean' to preserve the edit loop; --allow-untracked escape for spikes; guard no-ops outside a git repo so non-git fixtures/ad-hoc use are unaffected). Regression tests in tests/test_aet_work_add_review.py (untracked refused / tracked accepted / --allow-untracked bypass); full suite 631 green. Gaps 2 (make unattended hygiene fail-closed for dirty-tree/main-ahead) and 3 (add explicit commit-plans step to aet-pipeline-plan/aet-plan) routed to aet-plan. Bug doc: docs/bugs/2026-07-14-aet-add-queues-untracked-plans.md.", "prevents": "Queuing non-durable plans that vanish in an origin/main-based worktree at execution; and re-diagnosing the stale 'run-one skips hygiene' red herring \u2014 the real gap is the unattended-mode warn-and-continue plus the missing add-time guard."}
{"timestamp": "2026-07-14T15:20:00Z", "trigger": ["aet-work", "gate", "gate-submit", "tree_hash", "evidence", "schema-drift", "aet-qa", "writer-contract"], "problem": "`aet gate submit` rejects verdict payloads that follow the documented skill writer contract: pfe-01 made tree_hash a required key in all four verdict schemas and auto-stamps it in write_verdict, but aet-work/bin/gate._submit validates the RAW payload via validate_verdict before write_verdict can stamp it. The sanctioned writer (G1) exits 1 with \"Missing required key 'tree_hash'\" for every payload built per the aet-qa/review/cso SKILL.md contract, pushing all stages onto the hand-written fallback. Hit live during pfe-02's QA stage; pfe-01's session hit the same wall.", "layer": "aet-work/bin/gate (_submit validate-before-stamp ordering) vs aet-work/lib/evidence.py SCHEMAS (tree_hash required) + write_verdict (stamp-then-validate) + checking-skill SKILL.md verdict examples (no tree_hash)", "fix": "Not fixed in pfe-02 (out of plan scope; pre-existing on main). Workaround used: write via evidence.write_verdict(task_id, kind, record, path=resolve_verdict_path(...), worktree_dir=<worktree>) which stamps then validates. Fix direction: stamp tree_hash in gate._submit when absent, then validate, then write; regression tests in tests/test_gate_evidence.py. Bug doc: docs/bugs/2026-07-14-gate-submit-rejects-unstamped-payload.md. Queue as its own plan (gate is fail-closed G1 code).", "prevents": "Every checking stage (qa/review/cso/sync-docs) failing its verdict submission when the agent follows the documented payload contract, and re-diagnosing the drift from scratch each run."}
{"timestamp": "2026-07-14T15:44:29Z", "trigger": ["aet-work", "orchestrator", "create_worktree", "rebase-abort", "stale-diverged-branch", "branch-D-fails", "run-one", "worktree-py"], "problem": "create_worktree (aet-work/lib/worktree.py:94-131) salvages a stale diverged task branch by running `git rebase --onto <base> <merge-base> <branch>` with -C repo_root \u2014 i.e. in the MAIN worktree. Rebasing a non-current branch checks it out there; when the rebase conflicts, `rebase --abort` returns HEAD to the TASK branch, not back to the pre-rebase branch (main). The fallback `git branch -D <branch>` then fails ('cannot delete branch ... used by worktree at <repo_root>'), raising CalledProcessError that crashes run_one/batch at create_worktree before any stage runs. Reproduced twice with pfe-02-orchestrator-freshness-injection: a stale branch from a prior session (merge-base 2433804 != origin/main tip) whose rebase conflicted because pfe-01 content had landed on main squashed under a different SHA. Side effect: every crash silently leaves the user's main checkout switched to the task branch.", "layer": "aet-work/lib/worktree.py create_worktree stale-branch rebase/abort/branch-D path (lines ~94-131) + orchestrator run_single (no exception guard around create_worktree)", "fix": "Immediate recovery (applied 2026-07-14): `git switch main` + `git branch -D <task-branch>` (old commit preserved on origin/<branch> and in reflogs), then re-run \u2014 create_worktree takes its fresh-create path and the full pipeline completed (pfe-02 reached awaiting_merge). Structural (NOT applied \u2014 needs a plan): (a) run the salvage rebase inside a temp/detached worktree instead of repo_root, or (b) after abort, switch back to the pre-rebase branch before `branch -D`; and wrap create_worktree so a salvage failure can neither crash the run nor strand HEAD on the task branch.", "prevents": "Misreading a rebase-abort-checkout bug as a plain 'cannot delete branch' failure; losing run-one/batch runs to any stale diverged task branch; main checkout left on a task branch after a crashed run."}
{"timestamp": "2026-07-14T17:01:09Z", "trigger": ["aet-work", "orchestrator", "ttf-01", "plan-draft", "footer-stage", "intake-gap", "draft-plan", "instant-failure"], "problem": "ttf-01-wire-test-run-extraction failed twice in aet run, both times in seconds with no task telemetry log. Failure #1 (batch 96fc6bd4, 2.5s): the plan was queued untracked and never committed (lost draft), so the origin/main-built worktree lacked it and process_task failed closed at the 'plan not found in worktree' check (orchestrator:764). Failure #2 (run da83e6cf, 0.11s, final_stage=plan-draft): the plan recovered from session logs (c4377a1) kept footer '_Stage: plan-draft_' and 'status: draft'; get_current_stage (orchestrator:148) falls back to the footer, 'plan-draft' is not in the software workflow stage_map (entry is plan-approved), and the fresh worktree had 0 commits, so process_task returned False at the stage_map guard (orchestrator:792) before any stage ran. Both runs left a bare branch (0 unique commits) and a stale queue worktree field.", "layer": "docs/plans draft metadata vs aet-work/bin/orchestrator process_task stage_map guard + aet add/sync intake (accepts status: draft plans with non-runnable footer stages)", "fix": "Recovery path: (1) human review of the recovered plan content, then set frontmatter status: approved and footer _Stage: plan-approved_, commit and push; (2) after the live batch releases the lease, delete the bare ttf-01-wire-test-run-extraction branch (0 unique commits), clear the stale worktree field, transition failed->ready; (3) re-run. Structural candidate (NOT applied): aet add/sync should refuse (or loudly warn on) plans whose footer stage is not a runnable stage of their declared workflow — the same fail-fast philosophy as the untracked-plan guard added 2026-07-14.", "prevents": "Blind-retry loops on draft/unapproved plans that can never start (instant fail, no task log, bare-branch litter), and misreading a plan-metadata problem as an orchestrator regression."}
{"timestamp":"2026-07-15T07:09:35Z","trigger":["aet-work","orchestrator","aet install","worktree","broken symlink"],"problem":"A pipeline stage inside the vgr-04 worktree ran the aet installer (make install-skills / aet install), re-pointing the global ~/.local/bin/aet symlink at the ephemeral worktree copy (.worktrees/vgr-04-pytest-xdist-parallel/aet-work/bin/aet). When the worktree was removed after merge and closure, the aet CLI broke with a dangling symlink (command not found).","layer":"aet-work orchestrator stage environment + aet install","fix":"Restored by re-running make install-skills from the main checkout (it detected and repaired the stale symlink). Proper fix: the installer should refuse to re-point global symlinks when invoked from inside a .worktrees/ path, or the orchestrator should skip install steps in worktree sessions.","prevents":"Losing the aet CLI after routine worktree cleanup at the end of every pipeline run."}
{"timestamp": "2026-07-17T13:30:00Z", "trigger": ["aet-work", "orchestrator", "nsr-07", "stall-watchdog", "timeout", "cost", "failure-classification"], "problem": "The night-shift exit-gate rehearsal (nsr-07) failed because: (1) run_stage/run_stage_group hardcoded killed_by_timeout=False, so stall kills (exit -9) were classified as flaky instead of timeout; (2) on_failure=triage defaulted timeout to requeue, leaving the stall task stuck in ready state; (3) _finalize_task only rolled up per-task cost on the success path, so incident tasks had no cost on the ledger; (4) the fake test CLI did not flush stdout, so the usage envelope was lost when the stall watchdog killed the process.", "layer": "aet-work/bin/orchestrator _finalize_task, run_stage, run_stage_group; tests/test_nightshift_rehearsal.py fake CLI", "fix": "(a) Pass killed_by_timeout=(exit_code == -9) to _classify_failure in run_stage/run_stage_group. (b) In _finalize_task, read the latest failure-signature class and leave the task failed when it is timeout. (c) Write per-task cost from the child process in run_single for both success and failure, and call _write_task_cost on the failure path in _finalize_task. (d) Add sys.stdout.flush() after printing the usage envelope in the fake CLI.", "prevents": "Stall watchdog kills being misclassified and requeued forever; cost telemetry being silently dropped for failed/quarantined tasks."}
{"timestamp":"2026-07-19T19:46:08Z","trigger":["aet run","aet ship","aet evolve","timeout","background","awaiting_merge"],"problem":"Agent invoked aet run as a foreground command with the default timeout, causing an avoidable timeout; hesitated when the user said aet ship for awaiting_merge tasks; and tried to run aet evolve as an aet CLI subcommand, which does not exist.","layer":".agents/commands/aet-work.md","fix":"Created .agents/commands/aet-work.md with operational rules: always run aet run/run-one in background with long/disabled timeout; recognize awaiting_merge -> aet ship -> merge confirm -> ship helper closure workflow; and map skill names (aet-evolve) to their actual CLI entry points (aet retro) or skill activation.","prevents":"Repeated foreground timeouts, wasted clarification turns around shipping, and namespace confusion between AET skills and aet CLI subcommands."}
{"timestamp":"2026-07-20T23:36:26Z","trigger":["aet-implement","nc-03a-ship-gate-as-code","ATOMIC OVERSIZED"],"problem":"Plan nc-03a-ship-gate-as-code contains the ATOMIC OVERSIZED warning. The user explicitly instructed execution of the tdd→implement stage on this plan in auto permission mode.","layer":"aet-implement pre-flight size check","fix":"Proceeded under user direction; logged override here. The eight gate checks share one result/exit code and were implemented as one user-visible behavior.","prevents":"Blocking a user-directive for a plan the user has already approved."}
{"timestamp":"2026-07-23T23:03:55Z","trigger":["aet-ship","flexible CLI args","positional sniffing","review"],"problem":"First-pass fix for aet ship close used extension-sniffing on positional args, which silently admitted undocumented forms (task-id + queue as second arg) and ambiguous forms (two plan paths). aet-review caught both; initial tests covered only happy paths.","layer":"implementation discipline / aet-review","fix":"Constrained positionals in _normalize_close_args: second arg must be a .md plan unless the first arg is already a plan; queue belongs to the third positional only. Added rejection tests for ambiguous and misplaced arguments, plus the frontmatter id fallback path.","prevents":"Shipping flexible CLI forms that silently misinterpret user input and lack negative-path tests."}
{"timestamp": "2026-07-24T16:05:00+00:00", "trigger": ["aet-ship", "merge", "aet ship merge", "cfg-01", "cfg-02", "cfg-03"], "problem": "aet ship merge <task> reports success but only commits a plan-footer update on main; it does not merge the feature branch. The output 'Merging main into main' is the giveaway. This happened for cfg-01, cfg-02, and cfg-03 on 2026-07-24.", "layer": "aet ship merge command in src/aet/cli/ship.py (or equivalent merge implementation)", "fix": "Manual workaround: reset main, update plan footer in the worktree, git merge --no-ff <branch>, push. Structural fix needed: aet ship merge must resolve the task's actual feature branch as the merge source and verify git merge-base --is-ancestor <branch> main after merging.", "prevents": "Shipping tasks that appear merged while their implementation commits remain on unmerged feature branches, which stalls dependent tasks and corrupts main."}
{"timestamp": "2026-07-24T17:25:17Z", "trigger": ["aet-ship", "merge", "fail-closed", "prose-to-code regression", "src/aet/cli/ship.py"], "problem": "The post-merge --is-ancestor safety check for `aet ship merge` existed only as prose in aet-ship/SKILL.md (Steps 12-13, plan bs-01) and silently regressed when ship became Python code (nc-03a/b/c). Together with cmd_merge resolving the merge source from `git branch --show-current` instead of the task id, `aet ship merge` run from a checkout on main did a no-op 'Merging main into main' and recorded cfg-01/02/03 as merged without merging their code.", "layer": "cmd_merge / _merge_into_target in src/aet/cli/ship.py", "fix": "Resolve the feature branch by task id (_resolve_feature_branch, orchestrator's qt['branch']=task_id convention, local then origin/<id>); fail closed if unresolvable; pre-check feature_branch != target_branch; port the post-merge `git merge-base --is-ancestor` verification into _merge_into_target. Pre-check and post-condition are complementary: --is-ancestor alone cannot catch the bug because main is trivially an ancestor of main.", "prevents": "Silent data loss where a ship/merge command reports success and records closure while the feature branch is never incorporated; and safety invariants regressing invisibly when they live only in skill prose rather than in the code that enforces them."}
{"timestamp": "2026-07-26T00:00:00Z", "trigger": ["telemetry", "archive-layout", "project-slug", "glob", "mine-learnings", "aet-retro", "empty-archive"], "problem": "An agent measuring the telemetry corpus globbed ~/.aet/telemetry/{project}/*/*/*.jsonl (four levels) and got zero records for every project, because derive_project_slug (src/aet/project_id.py:40) returns <main-worktree-dir>/<worktree-label> \u2014 a slug spanning TWO path segments \u2014 so task logs sit five levels below the root (aiskills/main/2026-07-21/run-.../task.jsonl). The wrong depth fails silently: an empty match set reads as 'no telemetry', not 'bad path'. This is the second occurrence: on 2026-07-13 the same ambiguity made aet retro emit a malformed report (learning entry 38), fixed by tele-07-retro-reader-layout-fix, which disambiguated skills/aet-work/references/telemetry-log-schema.md but left docs/telemetry-guide.md's ASCII tree and CONTEXT.md's Telemetry Archive term showing the one-segment {project-slug} form.", "layer": "docs/telemetry-guide.md + CONTEXT.md (the two copies of the archive-layout contract that tele-07 did not update)", "fix": "Expanded the guide's ASCII tree to ~/.aet/telemetry/<main-worktree-dir>/<worktree-label>/{date}/{run-id}/ and added a paragraph naming the five-level depth and the silent-empty-match failure mode; rewrote CONTEXT.md's Telemetry Archive term to the five-level path with an _Avoid_ clause against writing {project-slug} as a single segment.", "prevents": "Re-deriving the archive depth wrongly and concluding the archive is empty. Generalises: when a contract is stated in several documents, fixing the authoritative copy is not fixing the contract \u2014 fix the copy named after the subject, which is the one an agent reads.", "recurrence": 2}
{"timestamp": "2026-07-26T00:00:01Z", "trigger": ["aet-metrics", "aet-retro", "first-pass-merge", "rework", "track_record", "test_run", "false-signal"], "problem": "aet metrics reports a 0.0% first-pass merge rate and 299 rework units (65 tasks since 2026-07-20), and the 2026-07-20 retro routed that as a factory-quality finding ('rework is universal'). It is a counting defect: metrics.py:116 calls track_record.rework_count, and _repeated_stage_count (track_record.py:103) groups by _stage_names over records that iter_telemetry_task_records (:74) yields as type in ('stage','test_run'); test_run records carry a stage field (telemetry.py:363), so every observed test run counts as a repeat of its own stage. Measured over 127 tasks: 121 (95%) show rework>0 versus 25 (20%) counting stage records only \u2014 418 phantom units \u2014 and 1 of 127 tasks (1%) passes both First-Pass Merge telemetry clauses versus 93 (73%). ADR-035 item 2 already says 'stage telemetry records', so the code contradicts its own spec.", "layer": "src/aet/track_record.py counting core (consumed by aet metrics and aet retro); ADR-052 records the correction", "fix": "Not yet applied. tap-01-factory-metrics-stage-records-only is ready in the queue and filters _has_failed_stage and _repeated_stage_count to type == 'stage', sequenced deliberately BEFORE the test-detection work (tap-02..tap-06) so the metric does not move as a side effect of detecting more test runs. Until it merges, do not cite aet metrics first-pass or rework figures as evidence, and re-baseline afterwards with the delta attributed per clause.", "prevents": "Routing a measurement artifact as a factory-quality finding, and letting a later detection improvement look like a quality regression.", "recurrence": 1}
{"timestamp": "2026-07-26T00:00:02Z", "trigger": ["plan-template", "work_class", "aet-metrics", "per-class-breakdown", "dead-dimension", "twe-01"], "problem": "aet metrics buckets by work_class and reports 65/65 tasks unclassified. plan_parser.py:242-246 reads work_class from plan frontmatter (trivial|normal|critical, default 'unclassified'), but .agents/templates/plan-template.md did not mention the key and 0 of 289 plans declared it; work-history.jsonl holds 238 records missing the field and 84 recording the literal 'unclassified', with zero classified records ever. twe-01-work-class-attribute merged the reader and validation without changing any layer that would cause a value to be written, so the 2026-07-20 retro's queued follow-up is merged but its stated purpose is unmet.", "layer": ".agents/templates/plan-template.md", "fix": "Added work_class: [trivial/normal/critical] to the template frontmatter with a comment stating it is the only source of the value and that omitting it keeps the aet metrics / aet retro per-class breakdown permanently empty. Backfilling the 289 existing plans was rejected \u2014 it would fabricate classifications nobody made at the time.", "prevents": "Shipping a metric dimension whose only writer is a field no author is ever told to set. Check that a new frontmatter attribute is added to the template in the same change as its parser.", "recurrence": 1}
{"timestamp": "2026-07-26T00:00:03Z", "trigger": ["aet-plan", "plan-validate", "plans-lint", "frontmatter-enum", "docs_sync", "status", "test-paths"], "problem": "Seven plans were authored and then failed lint four times: status: pending (not in PLAN_LIFECYCLE_STATUSES), docs_sync: conditional (only required|skipped accepted), invented test paths (tests/test_wirelog.py rather than the real tests/wirelog/test_wirelog.py), and test filenames placed on a bullet's continuation line \u2014 plan_validate._list_items reads only the bullet's own physical line, so a test file named on a wrapped line is invisible to the new-source acceptance rule.", "layer": ".agents/templates/plan-template.md (enum documentation) + agent behaviour (path verification)", "fix": "Extended the template's security_review/docs_sync comment to state that required and skipped are the ONLY accepted values, and that a stage whose need depends on the work's outcome should declare required and explain in the reason. The invented test paths and the continuation-line rule are behavioural, not systemic: list the target directory before citing paths in it, and read the lint rules before authoring a batch of plans rather than after.", "prevents": "Multi-cycle lint churn at the end of a planning session, and hedged enum values invented for genuinely conditional situations.", "recurrence": 1}
{"timestamp":"2026-07-28T10:16:27Z","trigger":["aet run","--max-jobs","rid-01","CLI dispatcher"],"problem":"`aet run` hard-coded `--max-jobs 4` and the only way to change concurrency was to invoke the orchestrator module directly, which the dispatcher is designed to prevent. This contradicted the user's expectation that batch concurrency should be caller-tunable from the supported CLI surface.","layer":"src/aet/cli/main.py + docs/plans/rid-01-detached-only-execution.md","fix":"Exposed `--max-jobs` as a Typer option on `aet run` (default 4) and forwarded it through `_build_orchestrator_flags`; left `--max-jobs` rejected on `aet run-one` and kept `--isolation`/`--stall-timeout` removed. Updated the rejection/forwarding tests and appended a post-implementation revision to rid-01 documenting the new behavior.","prevents":"Users being forced to bypass the supported CLI to control batch concurrency, and future agents re-adding `--max-jobs` to `run-one` where it has no batch meaning."}
{"timestamp":"2026-08-06T22:15:17Z","trigger":["aet-review","lop-02","seed_task_plan","resume","AET_WORKTREE"],"problem":"seed_task_plan staged the task plan and ran git commit unconditionally. On a resumed run or batch child that reuses an existing worktree (AET_WORKTREE), the plan file was already committed on the worktree branch with identical content, so git commit exited with nothing to commit and the task failed with RuntimeError.","layer":"src/aet/worktree.py","fix":"After staging, check git diff --cached --quiet and return False when the staged diff is empty. This keeps the first-seed behavior while tolerating worktree reuse.","prevents":"Resumed or batched runs failing on a plan that was already seeded. Verify seeding helpers handle the no-change case when a worktree may be reused.","recurrence":1}
{"timestamp": "2026-08-10T11:19:03Z", "trigger": ["aet sprint add", "plan_validate", "scope_findings", "ADR creation", "t2r-06", "t2r-08"], "problem": "aet sprint add intake rejected two valid plans (t2r-06, t2r-08) because scope_findings (plan_validate.py:340-351) regex-scans every `docs/adr/*.md` literal and requires it to exist on disk, with no exemption for files the plan itself creates. Plans whose deliverable IS a new ADR are a precedented pattern (nc-01, rdm-01, gib-01, vgr-02, pdh-01, cfg-05 all list `docs/adr/0XX-*.md (new)`). Workaround applied: reworded tasks/Files-to-Modify to `new file `056-...md` under `docs/adr/`` so the full-path literal no longer appears.", "layer": "src/aet/plan_validate.py scope_findings (d) scope-reference resolution", "fix": "Not applied (planning lockout). Proper fix: exempt paths declared as new deliverables in the plan's Files to Modify section (or an explicit `(new)` marker) from the ADR-resolution check, the same way new_files are already parsed at plan_validate.py:303-308 for the acceptance rule. Until then, authors of ADR-creating plans must split directory and filename in prose.", "prevents": "Intake rejecting legitimate ADR-creating plans, and authors gaming the path regex instead of the validator understanding the (new) convention.", "recurrence": 1}
{"timestamp":"2026-08-11T18:55:46Z","trigger":["aet run","--max-jobs 1","t2r-07-context-digest-injection","seed_task_plan","quarantine"],"problem":"After .gitignore began ignoring docs/plans/*.md (c8fd211, ADR-054 transient live plans), seed_task_plan staged the plan with a plain git add inside the task worktree; git refused the ignored path, the RuntimeError returned False with no failure signature, _finalize_task defaulted to class environment with empty context, and the triage agent quarantined the task before implementation. The closure path had already been force-add-fixed (95fb26e); the seed path was missed.","layer":"src/aet/worktree.py seed_task_plan + src/aet/cli/orchestrator.py process_task seed-failure path","fix":"Force-add the plan path in seed_task_plan (mirroring queue.py), record an environment failure signature with stage 'seed' and the stderr tail on seed failure, and added regression tests: a seed test whose fixture repo gitignores docs/plans/*.md, and an orchestrator test asserting the signature. ADR-054 gained an addendum: all plan-path staging must use git add -f.","prevents":"Batch runs quarantining tasks on gitignored live plans, and triage quarantining on empty failure context. When a design change makes a previously committable path gitignored, sweep every git add site that can touch it."}
