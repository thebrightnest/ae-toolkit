# Plan: Unify aet-work `run` with OS-Process Isolation

## Summary

Replace the broken cooperative `run` command and the `run-scripted` command with a single `run` command that uses OS-process isolation. Update all supporting reference documentation.

## Files to Modify

1. `aet-work/SKILL.md`
2. `aet-work/references/context-isolation.md`
3. `aet-work/references/orchestrator-template.sh`
4. `aet-work/references/afk-loop-orchestrator.sh`
5. `docs/use-cases.md` (if needed)

## Task List

### 1. Rewrite `aet-work/SKILL.md` — `run` command

- [x] Remove the old `run` section (cooperative loop with CLEAR CONTEXT)
- [x] Remove the `run-scripted` section entirely
- [x] Insert a new `run` section whose content is the former `run-scripted` procedure:
  - Runtime detection (kimi / claude / AGENT_CLI)
  - Generate orchestrator script from `aet-work/references/orchestrator-template.sh`
  - Spawn via `Shell(run_in_background=true)` and wait via `TaskOutput(block=true)`
  - Resume behavior (regenerate script, skip done/in-progress tasks)
- [x] Update Key Principles:
  - Remove "Two isolation levels" bullet
  - Remove "Context isolation via new sessions" bullet (or rewrite to say "OS-process isolation via orchestrator script")
  - Keep all other principles unchanged

### 2. Rewrite `aet-work/references/context-isolation.md`

- [x] Remove the "Two Kinds of Isolation" comparison table
- [x] Remove the "Cooperative Isolation — `aet-work run`" section
- [x] Rename "Guaranteed Isolation — `aet-work run-scripted`" to "OS-Process Isolation — `aet-work run`"
- [x] Update the runtime capability reference table: recommend `run` for all supported runtimes
- [x] Remove the "When to Use Which" table
- [x] Keep the "Without Context Isolation" and "With Context Isolation" tables (they explain the problem)
- [x] Update "Further Reading" links

### 3. Update orchestrator template header comments

- [x] `aet-work/references/orchestrator-template.sh`: change header comment from `generated by aet-work run-scripted` to `generated by aet-work run`

### 4. Update afk-loop reference comment

- [x] `aet-work/references/afk-loop-orchestrator.sh`: change line 16 comment from `Or use aet-work run-scripted` to `Or use aet-work run`

### 5. Check and update `docs/use-cases.md`

- [x] Scan for `run-scripted` references
- [x] Update or remove any mentions (none found in live files)

### 6. Create ADR 004

- [x] Write `docs/adr/004-unify-aet-work-run.md` documenting the decision to remove `run-scripted` with no alias

### 7. Validate and package

- [x] Run `make validate`
- [x] Run `make package`
- [x] Verify `aet-work.skill` contains the updated content

## Self-Validation Strategy

- `make validate` must pass (markdownlint + format-check + skill-structure validator)
- `make package` must regenerate `aet-work.skill` without errors
- `grep -r "run-scripted" aet-work/` must return zero matches (except in `.skill` zip if it contains old logs — acceptable)
- `aet-work/SKILL.md` line count must remain under 400 lines

## Locked-In Decisions

- No backward-compatible alias: `run-scripted` is fully removed
- No behavior changes to the orchestrator script logic
- Historical PRDs/plans/briefs in `docs/` are not edited

---

_Stage: merged_
_Next step: none — pipeline complete_
